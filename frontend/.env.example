# URL pública do seu backend (o servidor que você vai hospedar separadamente,
# ex: Railway, Render). Sem essa variável, o app tenta usar localhost, que só
# funciona no seu computador - não vai funcionar depois de publicado na Vercel.
VITE_API_URL=https://seu-backend-em-producao.com/api
